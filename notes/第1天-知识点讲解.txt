墨阁 · 第 1 天知识点讲解
零基础版 · 2026-09-23
============================================================

〇、今天学哪三个

学习工作台第一个目标「Python 基础」下面一共 12 个知识点，今天只学前 3 个：

    py1   变量与数据类型
    py2   列表 list
    py3   字典 dict

为什么是这三个，不是别的：

  · Python 的语法分两层。
    第一层是「数据」——怎么在程序里表示一条信息；
    第二层是「控制」——怎么处理这些信息（判断、循环、函数）。
    你今天项目里那份 db.py，出现频率最高的就是「数据」这一层的写法。

  · py4（if）和 py5（for）属于第二层，会用到「缩进」。
    缩进是零基础最容易翻车的地方，一天跨两层会糊。明天专门学。

今天学完你能做到什么：

    看懂 db.py 里大概一半的代码。
    不是全懂，是一半 —— 这已经很好了。
    剩下的一半（if / for / try / import）分别在 py4 py5 py8 py9。


============================================================
一、知识点 1：变量与数据类型
============================================================

1.1 变量是什么

变量 = 给一个值起个名字，以后用名字就能找到它。

比喻：像给抽屉贴标签。你把东西放进抽屉，贴上标签写着「九门素材」，
      以后要拿就说「把九门素材那个抽屉给我」，
      不用每次都描述「那个木头色的、第三个、里面装着民国资料的抽屉」。

写法：

    site_name = "墨阁"

这一行读作：把右边的 "墨阁" 装进左边这个名字叫 site_name 的盒子里。

最重要的一个认知：

    = 在这里不是数学里的「等于」，是「赋值」—— 把右边的装进左边。

    数学里 x = 5 表示 x 和 5 是一回事；
    程序里 x = 5 表示「把 5 存到 x 里」，x 以后还能改。

    x = 5
    x = 10
    print(x)      → 输出 10。后一次赋值把前一次盖掉了。


1.2 四种数据类型

Python 里的值分类型。今天你只需要认识四种：

    字符串 str     文字。必须用引号包起来。
                   "墨阁"     '九门旧事'     "2026"

    整数 int       不带小数点的数。
                   22     0     -5     10000

    小数 float     带小数点的数。
                   3.14     0.5     -1.5

    布尔 bool      只有两个值：True  和  False

注意几个零基础高频错：

  · 引号：单引号双引号都行，但要成对，而且必须是英文引号。
    中文引号 “ ” 会让程序直接报语法错 —— 长得像，但程序不认。

  · True / False 首字母必须大写。写成 true 会报错（程序不认识这个小写的名字）。

  · 数字加了引号就变成文字了：
        22      是整数，能算数
        "22"    是字符串，是「两个字」，不能算数

怎么知道自己手里的值是什么类型 —— 用 type()：

    print(type("墨阁"))      → <class 'str'>
    print(type(22))          → <class 'int'>
    print(type(3.14))        → <class 'float'>
    print(type(True))        → <class 'bool'>

现在不用懂 <class 'str'> 这个写法，看后面的单词就行：str / int / float / bool。


1.3 新手最大的坑：字符串和数字不能直接相加

    age = "22"        # 加了引号，这是字符串
    print(age + 1)    # 报错！

报错信息长这样：

    TypeError: can only concatenate str (not "int") to str

意思：字符串只能和字符串拼在一起，不能和整数加。

因为对程序来说，"22" 不是一个数，是「两个字」（字符 '2' 和字符 '2'）。
"22" + "33" 的结果是 "2233"（把字接起来），不是 55。

你工作台里 py1 的 why 写的正是这事：
「不懂类型，后面会频繁遇到『字符串加数字报错』这类问题。」

记一句话就够了：引号决定了它是文字还是数字。


1.4 f-string：把变量塞进句子里

这是 Python 打印东西最常用的方式，今天必须练到有肌肉记忆。

    site_name = "墨阁"
    print(f"欢迎来到{site_name}")

输出：

    欢迎来到墨阁

三个要点：

  · 引号前面有个小写字母 f —— 这是灵魂，少了它大括号里的东西不会被替换。

  · 变量用大括号 {} 包起来。

  · 大括号里可以直接写算式：
        print(f"一年有 {365 * 24} 小时")

如果没有那个 f：

    print("欢迎来到{site_name}")     → 输出：欢迎来到{site_name}
                                        （没替换，全是死的文字）

对比着写一遍，你就记住了。


1.5 在你项目里的出处

打开 backend/db.py，找这三行：

    第 40 行    BASE_DIR = os.path.dirname(...)      ← 变量，装一个文件夹路径
    第 47 行    DEFAULT_OWNER = "local"              ← 变量，装一个字符串
    第 106 行   return datetime.now().strftime(...)  ← 返回一个字符串

对应练习：day1_practice.py 的练习 1、2、3、4。


============================================================
二、知识点 2：列表 list
============================================================

2.1 列表是什么

列表 = 一排有顺序的东西，用中括号 [] 包起来，逗号分隔。靠「位置」取东西。

比喻：像一列车厢。每节车厢有编号，从头开始数。

    titles = ["九门旧事", "二月红传", "陈皮阿四"]

这一行：一个列表，里面装着三个字符串。


2.2 四个动作（你工作台 py2 的 key 就是这四个）

    titles = ["九门旧事", "二月红传", "陈皮阿四"]

    titles[0]                 → "九门旧事"                    取第一个
    titles[-1]                → "陈皮阿四"                    取最后一个
    titles[0:2]               → ["九门旧事", "二月红传"]       取前两个（切片）
    len(titles)               → 3                             一共几个
    titles.append("长沙夜雨")                                  往末尾加一个

术语先混个脸熟：

    titles[0]      这种带中括号的写法叫「索引」
    titles[0:2]    这种带冒号的写法叫「切片」
    append         是「往末尾追加」

切片有个反直觉的地方：

    titles[0:2]  拿的是第 0 个和第 1 个，**不包含第 2 个**。这叫「含头不含尾」。

    记法：0:2 表示「从 0 开始，数到 2 之前停」。


2.3 最大的坑：编号从 0 开始

零基础在这里翻车的比例最高。

    titles = ["九门旧事", "二月红传", "陈皮阿四"]
    #            第 0 个      第 1 个      第 2 个

    titles[0]    → "九门旧事"      第一个是 0，不是 1
    titles[1]    → "二月红传"
    titles[2]    → "陈皮阿四"
    titles[3]    → 报错！IndexError: list index out of range

为什么从 0 开始？这是计算机几十年的老规矩，原因不用深究，
但要养成习惯：看到 [0] 就是第一个，看到 [-1] 就是最后一个。

[-1] 特别好用：不用管列表里有几个，-1 永远是最后一个。
你项目里「取最新一条素材」就靠这种取法。


2.4 在你项目里的出处

打开 backend/db.py，找这三行：

    第 283 行   sql = ["SELECT DISTINCT m.* FROM materials m"]

        一个列表，装着 SQL 语句的片段。为什么用列表装？
        因为后面的条件（按标签筛、按关键词搜）要一段一段往里加，
        最后用 " ".join(sql) 拼成完整语句。
        列表天生适合「慢慢往里加东西」这种场景。

    第 284 行   params = []

        空列表。空的也要先建好，后面才能 append。

    第 198 行   return [r["name"] for r in rows]

        这是「列表推导式」，把 for 循环压成一行的高级写法。
        今天不用懂，明天学了 for 回来看就明白了。

对应练习：day1_practice.py 的练习 5。


============================================================
三、知识点 3：字典 dict
============================================================

3.1 字典是什么

字典 = 一堆带标签的信息，用大括号 {} 包起来，靠「名字」取东西。

比喻：像一张病历卡或者快递单。上面有「姓名」「年龄」「地址」这些栏目，
      你要找收件人，是按「姓名」这个栏目名去找，不是按「第几行」去找。

    material = {
        "标题": "九门旧事",
        "类型": "同人短篇",
        "来源": "九门资料库",
        "标签": ["民国", "师徒"],
    }

读法：这个字典有 4 个「键值对」。

    "标题" 是键（key），"九门旧事" 是它的值（value）。


3.2 取值

    material["标题"]     → "九门旧事"
    material["标签"]     → ["民国", "师徒"]

注意：取字典用中括号 + 引号包起来的键名。
为什么键名要加引号？因为它是一段文字（字符串）。
Python 里也允许不加引号的写法，但那是另一种东西，今天不碰。


3.3 今天最重要的一句话：列表 vs 字典

    列表靠「位置」找：  titles[0]          → 给第几个
    字典靠「名字」找：  material["标题"]   → 给哪个栏目

什么时候用哪个：

    一堆同类的东西、有先后顺序     → 列表    （书单、文件列表、标签列表）
    一个东西的多个属性、一一对应   → 字典    （一份素材的标题 / 类型 / 来源 / 标签）

再看一眼那个字典，你会发现「标签」这个键的值本身就是一个列表：

    "标签": ["民国", "师徒"]

这就是「字典里装列表」。反过来也可以「列表里装字典」：

    materials = [
        {"标题": "九门旧事", "字数": 3200},
        {"标题": "二月红传", "字数": 5100},
    ]

这叫套娃。它是所有数据结构的基本功，也是你项目里最核心的形态 ——
前端页面上那张素材列表，本质上就是这个样子。


3.4 在你项目里的出处（今天的重点）

打开 backend/db.py，看第 258 - 274 行：

    def _row_to_dict(conn, row, with_content=False):
        d = {
            "id": row["id"],
            "title": row["title"],
            "chars": row["chars"],
            ...
            "tags": tags_of(conn, row["id"]),
        }
        ...
        return d

这个函数的作用就是：把数据库里的一行记录，变成一个 Python 字典。

为什么？因为数据库的「行」只能按位置取，难用；
变成字典后就能按名字取，前端和接口都好处理。

看懂这一段，你就明白 db.py 一半在干什么了。

再看第 376 行：

    return {"materials": r["n"], "chars": r["c"], "tags": t["n"]}

这是页面上那个「素材数 / 总字数 / 标签数」统计条的来源。
它就是一个字典，三个键。

对应练习：day1_practice.py 的练习 6。


============================================================
四、今天怎么做（三步，大约 1 小时）
============================================================

第 1 步：先跑一遍，看结果（5 分钟）

    双击这个文件：

        G:\docker\moge\第1天练习.bat

    窗口里会刷出一段输出。你现在看不懂全部，正常。
    先感受一下「代码 → 输出」这件事是真的。


第 2 步：照抄、改一处、再跑（40 分钟）

    用记事本或 VS Code 打开：

        G:\docker\moge\notes\day1_practice.py

    里面有 7 段练习，每段都有【照抄这段】和【改一处】。

    做法：

      · 先把【照抄这段】的代码用手敲一遍（别复制，敲有敲的道理）
      · 再按【改一处】的提示改一下
      · 双击 第1天练习.bat，看输出变了没

    改错了不会坏。这个文件在 git 里有记录，改乱了我一条命令给你还原。


第 3 步：点亮工作台（5 分钟）

    · 打开学习工作台（workbench 文件夹里的 学习工作台.html）
    · 把 py1 变量与数据类型、py2 列表、py3 字典 三个点亮
    · 「工作日志」里随手写一句今天真懂了什么


============================================================
五、红字怎么读（零基础救命指南）
============================================================

看到一片红字别关窗口 —— 那是程序在告诉你问题在哪。

比如你写了：

    print(1 + "a")

运行后会输出：

    Traceback (most recent call last):
      File "notes/day1_practice.py", line 12, in <module>
        print(1 + "a")
    TypeError: unsupported operand type(s) for +: 'int' and 'str'

读法：从最后一行往上读。

    最后一行      TypeError: ...        ← 错误类型 + 原因。这是最重要的一行
    倒数第二行    print(1 + "a")        ← 出错的那句代码，原样贴给你看
    再上面        line 12               ← 出错在第 12 行

零基础会遇到的三种报错，先认识一下：

  SyntaxError（语法错）

      原因：漏引号、漏括号、用了中文标点。
      最常见：中文引号 “ ”、中文冒号 ：、中文括号 （）
      解决：把标点切成英文输入法，重新打一遍。

  IndentationError（缩进错）

      原因：行首的空格不对。Python 靠缩进判断代码的从属关系。
      今天你几乎碰不到，因为还没学 if / for。明天会遇到。

  NameError（名字错）

      原因：用了一个没定义过的名字。
      常见：变量名拼错了；或者忘了先给它赋值。
      名字大小写也敏感：Site_name 和 site_name 是两个不同的名字。


============================================================
六、今天的验收标准
============================================================

达标三条（都能做到就算过关）：

  1. 能不看教程说出一句话：

        「列表靠位置找，字典靠名字找」

  2. 能不看教程写出这两行：

        建一个变量装你的名字
        用 f-string 打印出「我是 XXX」

  3. day1_practice.py 跑通，7 段都动过，没有红字

只做到 1 和 2、没跑文件 —— 不算过关。

看懂不等于会。这个项目你最终要拿去面试，
面试官会问「这段为什么这么写」。


============================================================
七、明天学什么
============================================================

    py4   条件判断 if / else     让程序自己选路走
    py5   循环 for / while       对一堆东西重复做同一件事

明天的关键概念是「缩进」—— Python 最有特点、也最容易摔跤的地方。

今天先把「数据」这一层踩稳。

------------------------------------------------------------
这份文件随时可以重看。看不懂的地方直接问，不用自己硬啃。
